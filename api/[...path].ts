// Catch-all Vercel Serverless Function for the Express API.
// Keeping the /api prefix lets the frontend use the same relative URLs locally
// and in production.
export { default } from '../server.js';
